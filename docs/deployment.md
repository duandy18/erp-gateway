# Deployment Notes

This repository contains gateway configuration only.

It must not include business logic, business database access, or application code.

Production can use the same URL contract with managed Nginx, Caddy, or cloud load balancers.
